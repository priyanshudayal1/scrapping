# 📊 System Architecture Overview

## 🎯 High-Level Architecture

```
                    ┌─────────────────────────────────────┐
                    │   16.8M+ Judgement PDFs             │
                    │   168,867 Pages (100 results/page)  │
                    └─────────────────────────────────────┘
                                    │
                         ┌──────────┴──────────┐
                         │  Distributed into    │
                         │    66 Scripts        │
                         └──────────┬──────────┘
                                    │
        ┌───────────────┬───────────┼───────────┬───────────────┐
        │               │           │           │               │
    ┌───▼────┐     ┌───▼────┐  ┌───▼────┐  ┌───▼────┐
    │ VPS 1  │     │ VPS 2  │  │ VPS 3  │  │ VPS 4  │
    │Scripts │     │Scripts │  │Scripts │  │Scripts │
    │ 1-17   │     │ 18-34  │  │ 35-51  │  │ 52-66  │
    └───┬────┘     └───┬────┘  └───┬────┘  └───┬────┘
        │              │           │           │
        │              │           │           │
        └──────────────┴───────────┴───────────┘
                       │
                ┌──────▼──────┐
                │ AWS S3      │
                │ s3-vector-  │
                │ storage     │
                └─────────────┘
```

---

## 🏗️ Instance Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    VPS Instance                         │
│                                                         │
│  ┌───────────────────────────────────────────────┐    │
│  │         API Server (Port 5000)                │    │
│  │  • Flask REST API                             │    │
│  │  • Script Orchestrator                        │    │
│  │  • Web Dashboard                              │    │
│  └───────────┬───────────────────────────────────┘    │
│              │                                         │
│  ┌───────────▼───────────────────────────────────┐    │
│  │      Script Orchestrator                      │    │
│  │  • Manages multiple scripts                   │    │
│  │  • Sequential startup                         │    │
│  │  • Progress monitoring                        │    │
│  │  • Log aggregation                            │    │
│  └───────────┬───────────────────────────────────┘    │
│              │                                         │
│  ┌───────────▼───────────────────────────────────┐    │
│  │      Individual Scripts (17 per instance)     │    │
│  │                                               │    │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐     │    │
│  │  │Script│  │Script│  │Script│  │Script│ ... │    │
│  │  │  1   │  │  2   │  │  3   │  │  4   │     │    │
│  │  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘     │    │
│  │     │         │         │         │          │    │
│  │     ▼         ▼         ▼         ▼          │    │
│  │  [Progress] [Progress] [Progress] [Progress] │    │
│  │  [Timing]   [Timing]   [Timing]   [Timing]   │    │
│  │  [Logs]     [Logs]     [Logs]     [Logs]     │    │
│  └───────────────────────────────────────────────┘    │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 📱 Dashboard UI Flow

```
┌─────────────────────────────────────────────────────┐
│           Multi-Script Dashboard                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  🎮 Control Panel                                   │
│  ┌───────────────────────────────────────────┐    │
│  │ Scripts: [5] Delay: [2s] [▶️ Start]       │    │
│  │                       [⏹️ Stop All]        │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  📊 Overall Statistics                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│  │Total │ │Run   │ │Done  │ │Files │ │Prog  │   │
│  │  17  │ │  5   │ │  2   │ │125K  │ │11.5% │   │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘   │
│                                                     │
│  📈 Progress Bar                                    │
│  ████████░░░░░░░░░░░░░░░░░░░░░░░░░  11.5%        │
│                                                     │
│  🔍 Filter & Search                                 │
│  [All Scripts ▼] [Search...] [🔄 Refresh]         │
│                                                     │
│  📊 Scripts Grid (Individual Cards)                │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │
│  │ Script 1    │ │ Script 2    │ │ Script 3    │ │
│  │ 🟢 Running  │ │ 🟢 Running  │ │ ⚪ Not Start│ │
│  │ 15.2K files │ │ 12.5K files │ │ 0 files     │ │
│  │ Page 1523   │ │ Page 1250   │ │ Page 1      │ │
│  │ ████░ 59%   │ │ ███░░ 49%   │ │ ░░░░░ 0%    │ │
│  │ [⏹️ Stop]   │ │ [⏹️ Stop]   │ │ [▶️ Start]  │ │
│  │ [📄 Logs]   │ │ [📄 Logs]   │ │ [📄 Logs]   │ │
│  └─────────────┘ └─────────────┘ └─────────────┘ │
│                                                     │
│  ... (14 more cards)                               │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

```
┌─────────────┐
│   Browser   │
│  Dashboard  │
└──────┬──────┘
       │
       │ HTTP GET /api/status (every 5s)
       ▼
┌──────────────┐
│  API Server  │
│  (Flask)     │
└──────┬───────┘
       │
       │ get_overall_status()
       ▼
┌──────────────────┐
│  Orchestrator    │
│  • Query all     │
│    scripts       │
│  • Aggregate     │
│    progress      │
└──────┬───────────┘
       │
       │ Read JSON files
       ▼
┌──────────────────────┐
│  Script Files        │
│  • script1_progress  │
│  • script1_timing    │
│  • script1.log       │
│  • ... (x17)         │
└──────────────────────┘
       │
       │ Response
       ▼
┌─────────────┐
│   Browser   │
│   Updates   │
│   Display   │
└─────────────┘
```

---

## 🎬 Script Execution Flow

```
User clicks "Start 5 Scripts"
       │
       ▼
API receives POST /api/scripts/start
{num_scripts: 5, delay: 2}
       │
       ▼
Orchestrator.start_n_scripts(5, 2)
       │
       ├─► Start Script 1
       │   └─► subprocess.Popen(script1.py)
       │        └─► Running ✓
       │
       ├─► Wait 2 seconds
       │
       ├─► Start Script 2
       │   └─► subprocess.Popen(script2.py)
       │        └─► Running ✓
       │
       ├─► Wait 2 seconds
       │
       ├─► Start Script 3
       │   └─► subprocess.Popen(script3.py)
       │        └─► Running ✓
       │
       ├─► Wait 2 seconds
       │
       ├─► Start Script 4
       │   └─► subprocess.Popen(script4.py)
       │        └─► Running ✓
       │
       ├─► Wait 2 seconds
       │
       └─► Start Script 5
           └─► subprocess.Popen(script5.py)
                └─► Running ✓

Each Script:
1. Loads progress from JSON
2. Navigates to assigned page range
3. Downloads PDFs
4. Uploads to S3
5. Deletes local files
6. Updates progress JSON
7. Continues until end_page
```

---

## 📡 API Endpoint Map

```
┌─────────────────────────────────────────┐
│          API Endpoints                  │
├─────────────────────────────────────────┤
│                                         │
│  Script Control                         │
│  • POST /api/scripts/start              │
│    └─► Start N scripts sequentially     │
│  • POST /api/scripts/start/<id>         │
│    └─► Start single script              │
│  • POST /api/scripts/stop/<id>          │
│    └─► Stop single script               │
│  • POST /api/scripts/stop-all           │
│    └─► Stop all running scripts         │
│                                         │
│  Status & Monitoring                    │
│  • GET /api/status                      │
│    └─► Overall instance status          │
│  • GET /api/scripts/<id>/status         │
│    └─► Individual script status         │
│  • GET /api/scripts/<id>/logs           │
│    └─► Script log entries               │
│  • GET /api/instance/info               │
│    └─► Instance configuration           │
│  • GET /api/health                      │
│    └─► Health check                     │
│                                         │
│  Web UI                                 │
│  • GET /                                │
│    └─► Main dashboard                   │
│  • GET /unified                         │
│    └─► All-instances dashboard          │
│                                         │
└─────────────────────────────────────────┘
```

---

## 📊 Distribution Breakdown

```
Total: 16,886,658 Results
Total: 168,867 Pages (100 results/page)
Divided into: 66 Scripts (~2,559 pages each)
Deployed across: 4 VPS Instances

Instance 1 (VPS 1)
├── Scripts: 1-17 (17 scripts)
├── Pages: 1 to 43,503
├── Results: ~4,350,300
└── Time Est: ~1 month (parallel)

Instance 2 (VPS 2)
├── Scripts: 18-34 (17 scripts)
├── Pages: 43,504 to 87,006
├── Results: ~4,350,300
└── Time Est: ~1 month (parallel)

Instance 3 (VPS 3)
├── Scripts: 35-51 (17 scripts)
├── Pages: 87,007 to 130,509
├── Results: ~4,350,300
└── Time Est: ~1 month (parallel)

Instance 4 (VPS 4)
├── Scripts: 52-66 (15 scripts)
├── Pages: 130,510 to 168,867
├── Results: ~3,835,800
└── Time Est: ~1 month (parallel)

With all instances running in parallel:
Total Time: ~1 month (vs 3-4 months single instance)
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│          Security Layers                │
├─────────────────────────────────────────┤
│                                         │
│  1. Environment Variables               │
│     • AWS credentials in .env           │
│     • Email credentials in .env         │
│     • Never committed to git            │
│                                         │
│  2. API Access                          │
│     • CORS configured                   │
│     • Optional: API key auth            │
│     • Firewall: Restrict port 5000      │
│                                         │
│  3. VPS Security                        │
│     • SSH key authentication            │
│     • Firewall rules                    │
│     • Regular security updates          │
│                                         │
│  4. Data Protection                     │
│     • S3 bucket permissions             │
│     • Encrypted connections (HTTPS)     │
│     • Progress files backed up          │
│                                         │
└─────────────────────────────────────────┘
```

---

## 📈 Performance Metrics

```
Per Script:
• Pages: ~2,559
• Results: ~255,900 PDFs
• Time: ~1-2 weeks
• Rate: ~18,000 PDFs/day

Per Instance (17 scripts parallel):
• Pages: ~43,503
• Results: ~4,350,300 PDFs
• Time: ~1 month
• Rate: ~145,000 PDFs/day

All 4 Instances (66 scripts total):
• Pages: 168,867
• Results: 16,886,658 PDFs
• Time: ~1 month
• Rate: ~560,000 PDFs/day
```

---

## 🎯 Monitoring Points

```
┌─────────────────────────────────────────┐
│      What to Monitor                    │
├─────────────────────────────────────────┤
│                                         │
│  System Resources                       │
│  • CPU usage (keep < 80%)               │
│  • Memory usage (keep < 75%)            │
│  • Disk space (monitor S3 uploads)      │
│  • Network bandwidth                    │
│                                         │
│  Application Metrics                    │
│  • Scripts running                      │
│  • Download success rate                │
│  • Average time per file                │
│  • Failed downloads count               │
│                                         │
│  Progress Indicators                    │
│  • Current page per script              │
│  • Files downloaded per script          │
│  • Overall completion %                 │
│  • Estimated time remaining             │
│                                         │
│  Error Tracking                         │
│  • Script errors                        │
│  • S3 upload failures                   │
│  • Network timeouts                     │
│  • Captcha failures                     │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🚀 Deployment Workflow

```
1. Development
   └─► Create scripts locally
   └─► Test with 1-2 scripts
   └─► Verify S3 uploads

2. Testing
   └─► Deploy to single VPS
   └─► Run 3-5 scripts
   └─► Monitor for 24 hours
   └─► Validate data quality

3. Staging
   └─► Deploy to 2 VPS instances
   └─► Run 10-15 scripts total
   └─► Monitor for 48 hours
   └─► Check resource usage

4. Production
   └─► Deploy to all 4 VPS
   └─► Gradually scale to all 66 scripts
   └─► Monitor continuously
   └─► Adjust based on performance

5. Optimization
   └─► Analyze bottlenecks
   └─► Adjust concurrent scripts
   └─► Fine-tune delays
   └─► Optimize resource allocation
```

---

**System Ready for Deployment! 🎉**

Total Setup Time: ~30 minutes
Expected Runtime: ~1 month (4 VPS parallel)
Total PDFs: 16.8M+
Success Rate Target: >98%
